[project]
name = "claude-history-manager"
version = "0.1.0"
description = "Claude conversation history management system"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]

[tool.pytest.ini_options]
testpaths = ["tests"]
